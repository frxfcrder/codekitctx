# Optional: default repository path scanned by the API
REPOCTX_DEFAULT_PATH=.

# Optional: allow sensitive file inclusion (true/false)
REPOCTX_ALLOW_SENSITIVE=false

# Optional: max file size in bytes (default 1 MB)
REPOCTX_MAX_SIZE=1048576
